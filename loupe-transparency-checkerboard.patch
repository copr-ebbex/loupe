diff -Nur a/data/org.gnome.Loupe.gschema.xml.in b/data/org.gnome.Loupe.gschema.xml.in
--- a/data/org.gnome.Loupe.gschema.xml.in	2026-07-31 13:53:43.000000000 +0200
+++ b/data/org.gnome.Loupe.gschema.xml.in	2026-08-26 13:47:51.874195857 +0200
@@ -5,5 +5,9 @@
       <default>false</default>
       <summary>Show the image properties sidebar</summary>
     </key>
+    <key name="checkerboard-background" type="b">
+      <default>false</default>
+      <summary>Draw a checkerboard behind images with transparency</summary>
+    </key>
   </schema>
 </schemalist>
diff -Nur a/src/widgets/image/background_color.rs b/src/widgets/image/background_color.rs
--- a/src/widgets/image/background_color.rs	2026-07-31 13:53:43.000000000 +0200
+++ b/src/widgets/image/background_color.rs	2026-08-26 13:47:51.874379361 +0200
@@ -30,8 +30,12 @@
         (*self.background_color.borrow()).unwrap_or_else(Self::default_background_color)
     }
 
-    pub(super) fn set_background_color(&self, color: Option<gdk::RGBA>) {
-        self.background_color.replace(color);
+    /// Stores the guessed background color and whether the image contains
+    /// transparent pixels
+    pub(super) fn set_background_color(&self, color: Option<(gdk::RGBA, bool)>) {
+        let has_transparency = color.as_ref().is_some_and(|(_, t)| *t);
+        self.has_transparency.set(has_transparency);
+        self.background_color.replace(color.map(|(color, _)| color));
     }
 
     pub fn default_background_color() -> gdk::RGBA {
@@ -50,12 +54,46 @@
         }
     }
 
+    /// Colors of the checkerboard drawn behind images that contain
+    /// transparent pixels
+    pub fn checkerboard_colors() -> (gdk::RGBA, gdk::RGBA) {
+        if adw::StyleManager::default().is_dark() {
+            (BACKGROUND_COLOR_DEFAULT, CHECKERBOARD_SECONDARY_COLOR)
+        } else {
+            (
+                BACKGROUND_COLOR_DEFAULT_LIGHT_MODE,
+                CHECKERBOARD_SECONDARY_COLOR_LIGHT_MODE,
+            )
+        }
+    }
+
+    /// Whether the user enabled the checkerboard backdrop; lazily connects
+    /// to the setting so toggling repaints live
+    pub(super) fn checkerboard_enabled(&self) -> bool {
+        let settings = self.app_settings.get_or_init(|| {
+            let settings = LpApplication::default().settings();
+            let obj = self.obj().clone();
+            settings.connect_changed(
+                Some("checkerboard-background"),
+                glib::clone!(
+                    #[weak]
+                    obj,
+                    move |_, _| {
+                        obj.queue_draw();
+                    }
+                ),
+            );
+            settings
+        });
+        settings.boolean("checkerboard-background")
+    }
+
     /// Returns a background color that should give suitable contrast with
     /// transparent images
     ///
     /// For non-transparent images this always returns
     /// `BACKGROUND_COLOR_DEFAULT`
-    pub async fn background_color_guess(&self) -> Option<gdk::RGBA> {
+    pub async fn background_color_guess(&self) -> Option<(gdk::RGBA, bool)> {
         let obj = self.obj();
 
         tracing::debug!("Determining background color");
@@ -67,7 +105,7 @@
         // Shortcut for formats that don't support transparency
         if !obj.metadata().is_potentially_transparent() {
             tracing::trace!("This format does not support transparency");
-            return Some(Self::default_background_color());
+            return Some((Self::default_background_color(), false));
         }
 
         let (width, height) = self.untransformed_dimensions();
@@ -152,7 +190,7 @@
 
             if !has_transparency {
                 tracing::trace!("This image does not have transparency");
-                return Some(default_color);
+                return Some((default_color, false));
             }
 
             let n_pixels = texture.width() * texture.height();
@@ -167,9 +205,9 @@
             tracing::trace!("Amount bad contrast: {part_bad_contrast}");
 
             if part_bad_contrast > BACKGROUND_GUESS_LOW_CONTRAST_TRHESHOLD {
-                Some(alternate_color)
+                Some((alternate_color, true))
             } else {
-                Some(default_color)
+                Some((default_color, true))
             }
         })
         .await
diff -Nur a/src/widgets/image/rendering.rs b/src/widgets/image/rendering.rs
--- a/src/widgets/image/rendering.rs	2026-07-31 13:53:43.000000000 +0200
+++ b/src/widgets/image/rendering.rs	2026-08-26 13:47:51.874469632 +0200
@@ -84,10 +84,18 @@
             gsk::ScalingFilter::Nearest
         };
 
+        let checkerboard = self.has_transparency.get() && self.checkerboard_enabled();
+
         let render_options = tiling::RenderOptions {
             scaling_filter,
             scaling: self.scaling(),
-            background_color: Some(self.background_color()),
+            // With the checkerboard, transparent image pixels must not be
+            // backfilled per tile so the checkerboard shows through
+            background_color: if checkerboard {
+                None
+            } else {
+                Some(self.background_color())
+            },
         };
 
         // Operations on snapshots are coordinate transformations
@@ -95,10 +103,23 @@
         snapshot.save();
 
         // Add background
-        snapshot.append_color(
-            &self.background_color(),
-            &graphene::Rect::new(0., 0., widget_width as f32, widget_height as f32),
-        );
+        let viewport_rect =
+            graphene::Rect::new(0., 0., widget_width as f32, widget_height as f32);
+        if checkerboard {
+            // Repeat a 2x2-square cell over the whole viewport. Coordinates
+            // are logical widget pixels; the pattern is anchored to the
+            // viewport and does not move with panning or zooming.
+            let (primary, secondary) = Self::checkerboard_colors();
+            let size = CHECKERBOARD_SQUARE_SIZE;
+            let cell = graphene::Rect::new(0., 0., 2. * size, 2. * size);
+            snapshot.push_repeat(&viewport_rect, Some(&cell));
+            snapshot.append_color(&primary, &cell);
+            snapshot.append_color(&secondary, &graphene::Rect::new(0., 0., size, size));
+            snapshot.append_color(&secondary, &graphene::Rect::new(size, size, size, size));
+            snapshot.pop();
+        } else {
+            snapshot.append_color(&self.background_color(), &viewport_rect);
+        }
 
         // Apply the scrolling position to the image
         let hadj: gtk::Adjustment = obj.hadj();
diff -Nur a/src/widgets/image.rs b/src/widgets/image.rs
--- a/src/widgets/image.rs	2026-07-31 13:53:43.000000000 +0200
+++ b/src/widgets/image.rs	2026-08-26 13:47:51.874303833 +0200
@@ -55,6 +55,7 @@
 use glib::{Properties, SignalGroup};
 use gufo::common::orientation::Orientation;
 
+use crate::application::LpApplication;
 use crate::decoder::tiling::SharedFrameBuffer;
 use crate::decoder::{self, Decoder, DecoderUpdate, tiling};
 use crate::deps::*;
@@ -74,6 +75,17 @@
 const BACKGROUND_COLOR_ALTERNATE_LIGHT_MODE: gdk::RGBA =
     gdk::RGBA::new(101. / 255., 101. / 255., 105. / 255., 1.);
 
+/// Lighter squares of the checkerboard drawn behind transparent images in
+/// dark mode `#33333a`
+const CHECKERBOARD_SECONDARY_COLOR: gdk::RGBA =
+    gdk::RGBA::new(51. / 255., 51. / 255., 58. / 255., 1.);
+/// Darker squares of the checkerboard drawn behind transparent images in
+/// light mode `#dedee2`
+const CHECKERBOARD_SECONDARY_COLOR_LIGHT_MODE: gdk::RGBA =
+    gdk::RGBA::new(222. / 255., 222. / 255., 226. / 255., 1.);
+/// Edge length of one checkerboard square in logical pixels
+const CHECKERBOARD_SQUARE_SIZE: f32 = 10.;
+
 /// Consider 3.5:1 contrast and worse to be bad contrast for a pixel
 static BACKGROUND_GUESS_LOW_CONTRAST_RATIO: f32 = 3.5;
 /// Consider transparent images with more than 90% pixels bad contrast as bad
@@ -169,6 +181,11 @@
         #[property(get)]
         pub(super) is_loaded: Cell<bool>,
         pub(super) background_color: RefCell<Option<gdk::RGBA>>,
+        /// Whether the last background color analysis found pixels with
+        /// alpha < 255; enables the checkerboard backdrop
+        pub(super) has_transparency: Cell<bool>,
+        /// App settings, initialized lazily on first snapshot
+        pub(super) app_settings: OnceCell<gio::Settings>,
         pub(super) fixed_background_color: RefCell<Option<gdk::RGBA>>,
         /// Animations disabled
         pub(super) still: Cell<bool>,
